<div>
	<h1><%= @user.full_name %></h1>

	<% @statuses.each do |status|%>
		<div class="well">
			<%= status.content %>
			<hr>
			<%= link_to time_ago_in_words(status.created_at), status %>
		</div>
	<% end%>
</div>
